const randomNumber = Math.trunc(Math.random() *100 +1 )


document.querySelector(".check-btn").addEventListener("click", () => {
  const guessInput = Number(document.querySelector(".guess-input").value)
  console.log(tguessInput)
})